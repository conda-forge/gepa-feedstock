context:
  version: "0.0.27"
  python_min: 3.10

package:
  name: gepa
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/g/gepa/gepa-${{ version }}.tar.gz
    sha256: 02ecb19e4aa6a1f5bb2994cd54b2057f25cd5e8cd662fee514303b2a8ba0a738

build:
  number: 0
  script: ${{ PYTHON }} -m pip install .
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - setuptools >=77.0.1
    - wheel
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      python_version: ${{ python_min }}.*
      imports:
        - gepa
      pip_check: true

about:
  homepage: https://github.com/gepa-ai/gepa
  summary: A framework for optimizing textual system components (AI prompts, code snippets, etc.) using LLM-based reflection and Pareto-efficient evolutionary search.
  license: MIT
  license_file: LICENSE
extra:
  recipe-maintainers:
    - moritzwilksch
